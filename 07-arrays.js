// Arrays store multiple values in a single variable.

const passwords = ["", "12345", "SecurePass123!"];

// Access array items by their index.
console.log(passwords[0]);
console.log(passwords[1]);
console.log(passwords[2]);

// Check how many items the array contains(how many passwords are stored).
console.log(passwords.length);