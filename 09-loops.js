// Loops execute the same block of code multiple times.

const passwords = ["", "12345", "SecurePass123!"];

// Iterate through each password in the array.
for (const password of passwords) {
  console.log(password);
}